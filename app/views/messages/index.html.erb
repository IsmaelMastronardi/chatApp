<h1>CHAT INDEX</h1>

<div id="messages">
  <%= render @messages %>
</div>

<%= form_with model: @message, remote: true do |form| %>
  <div class="field">
    <%= form.text_area :content %>
  </div>
  <div class="actions">
    <%= form.submit 'send' %>
  </div>
<% end %>
